<div class="fixed top-0 md:top-16 right-0 z-20 w-full md:w-auto max-w-md text-center text-white text-sm">
  <% flash.each do |name, msg| %>
    <div class="bg-red-600 m-2 py-4 px-6 rounded-md shadow-xl">
      <%= msg %>
    </div>
  <% end %>
</div>
